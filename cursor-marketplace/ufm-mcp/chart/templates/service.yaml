apiVersion: v1
kind: Service
metadata:
  name: {{ include "ufm-mcp.fullname" . }}
  labels:
    {{- include "ufm-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ufm-mcp.selectorLabels" . | nindent 4 }}
