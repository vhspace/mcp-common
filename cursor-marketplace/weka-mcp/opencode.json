{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "weka-mcp": {
      "type": "local",
      "command": [
        "uvx",
        "--from",
        "weka-mcp",
        "weka-mcp"
      ],
      "environment": {
        "WEKA_HOST": "${WEKA_HOST}",
        "WEKA_PASSWORD": "${WEKA_PASSWORD}"
      },
      "enabled": true
    }
  }
}
