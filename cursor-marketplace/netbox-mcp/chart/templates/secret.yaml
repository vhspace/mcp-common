{{- if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "netbox-mcp.fullname" . }}
  labels:
    {{- include "netbox-mcp.labels" . | nindent 4 }}
type: Opaque
stringData:
  NETBOX_TOKEN: {{ .Values.netbox.token | quote }}
  {{- if .Values.mcpHttpAccessToken }}
  MCP_HTTP_ACCESS_TOKEN: {{ .Values.mcpHttpAccessToken | quote }}
  {{- end }}
{{- end }}
