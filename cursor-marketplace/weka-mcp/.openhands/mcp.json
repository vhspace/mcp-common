{
  "mcpServers": {
    "weka-mcp": {
      "command": "uvx",
      "args": [
        "--from",
        "weka-mcp",
        "weka-mcp"
      ],
      "env": {
        "WEKA_HOST": "${WEKA_HOST}",
        "WEKA_PASSWORD": "${WEKA_PASSWORD}"
      }
    }
  }
}
