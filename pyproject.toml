[project]
name = "mcp-common"
version = "0.39.1"
description = "Shared utilities and testing infrastructure for MCP server projects"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
keywords = ["mcp", "model-context-protocol", "fastmcp", "testing"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
]
dependencies = [
    "fastmcp>=3.4.2,<4",
    "httpx>=0.27",
    "pydantic-settings>=2.14.1",
    "python-dotenv>=1.2.2",
    "starlette>=1.3.1",
    "typer>=0.15.0",
]

[project.scripts]
mcp-plugin-gen = "mcp_common.plugin_cli:main"
mcp-common-doctor = "mcp_common.doctor:main"

[project.optional-dependencies]
testing = [
    "pytest>=9.0.3",
    "anyio>=4.13.0",
    "pytest-httpx>=0.36.2",
    "inline-snapshot>=0.34.0",
]
eval = [
    "inspect-ai>=0.3.241",
    "openai>=2.40.0",
    "tenacity>=8.0",
]
eval-scoring = [
    "deepeval>=4.0.7",
]
eval-tracking = [
    "wandb>=0.27.0",
]

[dependency-groups]
dev = [
    "mcp-common[testing]",
    "mcp-common[eval]",
    "mypy>=2.1.0",
    "ruff>=0.15.15",
    "pre-commit>=4.6.0",
]

[build-system]
requires = ["uv_build>=0.11.18,<0.12.0"]
build-backend = "uv_build"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "SIM", "RUF"]
ignore = ["E402", "E501", "SIM102", "SIM105", "SIM108", "SIM117", "UP047"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true

# Optional eval backends (eval-scoring / eval-tracking extras) ship no type
# stubs and are not installed in the default dev/CI sync, so their deferred
# imports would otherwise trip "missing import" under strict mode.
[[tool.mypy.overrides]]
module = ["deepeval.*", "wandb.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "unit: unit tests",
    "integration: integration tests",
    "e2e: end-to-end tests",
    "eval: LLM-as-judge evaluation tests (requires mcp-common[eval])",
]
