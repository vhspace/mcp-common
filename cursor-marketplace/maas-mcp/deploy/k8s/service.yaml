apiVersion: v1
kind: Service
metadata:
  name: maas-mcp
  namespace: maas-mcp
  labels:
    app.kubernetes.io/name: maas-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: maas-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
