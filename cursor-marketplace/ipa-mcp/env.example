# IPA MCP Server Configuration
# Copy this file to .env and fill in your values

# Required: FreeIPA server hostname or URL
IPA_HOST=ipa.example.com

# Required: IPA admin credentials
IPA_USERNAME=admin
IPA_PASSWORD=your-password-here

# Optional: SSL verification (default: false for self-signed certs)
IPA_VERIFY_SSL=false
